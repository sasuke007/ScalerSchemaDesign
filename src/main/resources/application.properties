spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:mysql://localhost:3306/students_schema
spring.datasource.username=springuser
spring.datasource.password=sasuke007
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.show-sql=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.show-actuator=true
#management.endpoints.web.exposure.include=*
spring.sql.init.mode=always
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
#hibernate.dialect.storage_engine=innodb
spring.jpa.database-platform= org.hibernate.dialect.MySQL5InnoDBDialect

#to disable the creation of schema by hibernate useful when making schema through schema.sql and data.sql files.
#spring.jpa.hibernate.ddl-auto=none

#Also, script-based initialization is performed by default only for embedded databases, to always initialize a database using scripts, we'll have to use:
#spring.sql.init.mode=always # takes 3 values always,embedded(default),never.


